### Code starts here ###=
y  = 10

# define functions
def sum_of_square (y) :
  sum = 0 
  for z in range (1,y+1) :
    sum = z**2 + sum
  return sum

def square_of_sum (y) :
  sum = 0
  for z in range (1,y+1) :
    sum = z + sum
  return (sum**2)

minus = square_of_sum(y) - sum_of_square(y) 
print (minus)
